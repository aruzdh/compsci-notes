#import "../../lib.typ": *

== Calcular la Probabilidad de un Evento 

Sea $Omega$ el espacio muestral de un experimento aleatorio. Supongamos que $Omega$ es un conjunto numerable finito. Si $A$ es un evento, entonces *la probabilidad de A* se calcula como sigue.

$ P(A) = abs(A)/abs(Omega) $

