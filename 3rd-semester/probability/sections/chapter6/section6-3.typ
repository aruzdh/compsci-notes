#import "../../lib.typ":*

== Desviación Estándar

#definition("Desviación Estándar")[
  Sea $cal(X)$ una variable aleatoria con varianza $"Var"(cal(X))$, entonces se define la *desviación estándar* de $cal(X)$ de la siguiente forma.
  $ sigma = sqrt("Var"(cal(X))) $
]
